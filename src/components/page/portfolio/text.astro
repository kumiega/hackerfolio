---
import { marked } from "marked";

interface Props {
  content: string;
}

const { content } = Astro.props;

// Convert markdown to HTML
const htmlContent = marked(content);
---

<div class="space-y-4 text-foreground text-base max-w-prose prose prose-sm prose-invert" set:html={htmlContent} />
